const initialState = {
  names: [
    { id: 1, name: "Andrey" },
    { id: 2, name: "Sasha" },
    { id: 3, name: "Sveta" },
  ],
};

const sitebarReducer = (state = initialState, action) => {
  return state;
};

export default sitebarReducer;
